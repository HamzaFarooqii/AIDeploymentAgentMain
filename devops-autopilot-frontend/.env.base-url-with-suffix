# Test fixture only: loaded by Vite's `loadEnv` for the vitest
# "base-url-with-suffix" project (see vitest.config.ts), which exercises
# src/api/client.ts's base-URL resolution when VITE_API_BASE_URL already ends
# with /api. Not used by `npm run dev` / `npm run build` (those use the
# default mode).
VITE_API_BASE_URL=https://example.com/api
